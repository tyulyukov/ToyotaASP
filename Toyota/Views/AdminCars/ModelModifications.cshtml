@model IEnumerable<Toyota.Data.Entities.Modification>

@{
    ViewData["Title"] = "Modifications for " + @ViewBag.OpenModel.Name;
}

<a asp-action="Index">Back to List</a>

<h1>Model: @ViewBag.OpenModel.Name</h1>

<div class="row">
    @foreach (var modification in Model)
    {
        <div class="col-3">
            <div class="card" style="padding: 10px; margin-bottom: 30px;">
                <img class="card-img-top" src="@modification.ImgUrl" />
                <div class="card-body">
                    <h5 class="card-title">@modification.Name</h5>
                    <a asp-action="ModificationColors" asp-route-id="@modification.Id" class="btn btn-primary">Colors</a>
                </div>
            </div>
        </div>
    }
</div>

<div class="text-center" style="margin-top: 15px;">
    <!-- Триггер/Открыть модальный -->
    <button style="border-radius: 20px; margin: 0 auto;" class="btn btn-outline-info" onclick="document.getElementById('myModal-create-modification').style.display = 'block';">Create</button>
</div>

<!-- Модальный -->
<div id="myModal-create-modification" class="modal">
    <!-- Модальное содержание -->
    <div class="modal-content">
        <div class="modal-header">
            <span class="close" onclick="document.getElementById('myModal-create-modification').style.display = 'none';">&times;</span>
            <h2>Создание модификации</h2>
        </div>
        <form asp-action="CreateModification" enctype="multipart/form-data">
            <div class="modal-body">
                <input type="hidden" name="ModelId" value="@ViewBag.OpenModel.Id" />

                <div class="form-group input-group-sm">
                    <label class="control-label">Slug</label>
                    <input name="Slug" class="form-control" />

                    <label class="control-label">Name</label>
                    <input name="Name" class="form-control" />

                    <label class="control-label">Image</label>
                    <input name="file" type="file" class="form-control" />
                </div>
            </div>
            <div class="modal-footer">
                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-default" />
                </div>
            </div>
        </form>
    </div>
</div>


<style>
    /* Модальный (фон) */
    .modal {
        display: none; /* Скрыто по умолчанию */
        position: fixed; /* Оставаться на месте */
        z-index: 1; /* Сидеть на вершине */
        left: 0;
        top: 0;
        width: 100%; /* Полная ширина */
        height: 100%; /* Полная высота */
        /*overflow: auto; Включите прокрутку, если это необходимо */
        background-color: rgb(0,0,0); /* Цвет запасной вариант */
        background-color: rgba(0,0,0,0.4); /* Черный с непрозрачностью */
    }

    /* Модальное содержание/коробка */
    .modal-content {
        background-color: #fefefe;
        margin: 15% auto; /* 15% сверху и по центру */
        padding: 20px;
        border: 1px solid #888;
        width: 80%; /* Может быть больше или меньше, в зависимости от размера экрана */
    }

    /* Кнопка закрытия */
    .close {
        color: #aaa;
        float: right;
        font-size: 28px;
        font-weight: bold;
    }

    .close:hover,
    .close:focus {
        color: black;
        text-decoration: none;
        cursor: pointer;
    }
</style>